import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  backdrop: {
    flex: 1,
    flexDirection: 'row',
  },
  button: {
    flex: 1,
    margin: 100,
    alignItems: 'center',
  }
});

export default styles;
